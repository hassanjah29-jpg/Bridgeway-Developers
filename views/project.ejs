<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= project.title %> | <%= c.site.brandName %></title>
    <meta name="description" content="<%= project.description %>" />
    <meta name="theme-color" content="<%= c.site.themeColor %>" />
    <meta property="og:title" content="<%= project.title %> | <%= c.site.brandName %>" />
    <meta property="og:description" content="<%= project.description %>" />
    <meta property="og:image" content="<%= project.image %>" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/styles.css?v=2" />
  </head>
  <body>
    <% if (preview) { %>
    <div style="position:fixed;z-index:9999;bottom:18px;left:18px;background:#16273d;color:#f7e98e;padding:10px 16px;border-radius:30px;font:600 13px/1 Inter,sans-serif;box-shadow:0 8px 24px rgba(0,0,0,.3)">
      Preview — unpublished draft · <a href="/admin" style="color:#fff;text-decoration:underline">back to admin</a>
    </div>
    <% } %>

    <!-- ============ NAVBAR ============ -->
    <header class="navbar scrolled" id="navbar">
      <div class="container nav-inner">
        <a href="/" class="logo" aria-label="Bridgeway Developers home">
          <svg class="nav-logo" viewBox="0 0 470 250" aria-hidden="true"><defs><linearGradient id="bwGradNav" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#c0922c"/><stop offset="1" stop-color="#8a641c"/></linearGradient></defs><path fill-rule="evenodd" fill="url(#bwGradNav)" d="M 76 28.905 L 51.500 29.500 85 30.077 C 115.737 30.606, 118.929 30.824, 123.698 32.721 C 130.658 35.491, 134.372 38.265, 139.059 44.194 C 146.106 53.109, 148.847 61.844, 149.709 78.134 C 150.333 89.918, 148.697 99.279, 144.397 108.535 C 140.608 116.692, 138.001 118.431, 126.500 120.476 C 113.787 122.737, 103.255 125.252, 95 127.999 C 88.335 130.217, 62.898 140.974, 62 141.954 C 61.206 142.821, 57.697 144.919, 55.309 145.955 C 53.314 146.821, 35.177 158.758, 34 159.981 C 33.725 160.266, 30.913 162.672, 27.750 165.327 C 24.587 167.982, 22 170.570, 22 171.077 C 22 172.768, 24.454 172.023, 29 168.952 C 33.344 166.018, 35.536 164.900, 48.500 159.008 C 57.692 154.830, 65.063 152.446, 82.471 148.023 C 101.598 143.162, 115.360 140.238, 127.450 138.467 C 138.272 136.882, 139.231 137.242, 147.254 145.908 C 155.574 154.894, 159.009 165.853, 159.109 183.731 C 159.245 208.018, 146.511 225.405, 125.325 229.859 C 122.098 230.537, 103.935 230.979, 77.075 231.032 C 53.384 231.079, 34 231.488, 34 231.941 C 34 233.390, 119.912 234.411, 127.680 233.053 C 147.130 229.654, 158.255 224.586, 169.267 214.110 C 172.438 211.092, 176.354 206.566, 177.967 204.052 C 185.005 193.084, 185.975 174.610, 180.175 162 C 176.221 153.405, 164.580 142.229, 155.727 138.531 C 151.139 136.614, 150.979 135.270, 155.250 134.521 C 161.385 133.445, 195.502 131.013, 204.543 131.006 L 213.587 131 217.793 142.291 C 220.107 148.500, 222 154.131, 222 154.804 C 222 155.477, 222.435 156.809, 222.967 157.764 C 224.967 161.358, 238 198.147, 238 200.200 C 238 200.970, 238.423 202.028, 238.941 202.550 C 239.458 203.073, 241.763 209.125, 244.062 216 C 246.361 222.875, 248.693 229.063, 249.244 229.750 C 249.794 230.438, 251.710 231, 253.501 231 C 256.261 231, 256.941 230.505, 257.962 227.750 C 258.624 225.963, 259.803 223.022, 260.583 221.216 C 261.362 219.410, 262 217.137, 262 216.166 C 262 215.195, 262.373 213.972, 262.830 213.450 C 263.689 212.466, 269.165 197.732, 276.192 177.500 C 277.338 174.200, 278.647 170.600, 279.102 169.500 C 279.557 168.400, 282.488 160.300, 285.616 151.500 C 288.744 142.700, 291.585 135.172, 291.929 134.772 C 292.471 134.143, 315.733 136.445, 318.504 137.402 C 319.056 137.592, 320.101 139.492, 320.826 141.624 C 321.552 143.756, 323.414 148.875, 324.964 153 C 327.732 160.365, 330.691 168.606, 336.993 186.500 C 342.596 202.409, 350.064 222.378, 352.047 226.750 C 353.587 230.147, 354.490 231, 356.543 231 C 359.644 231, 360.568 229.116, 368.341 206.929 C 382.729 165.859, 386.118 156.386, 386.917 155 C 388.130 152.898, 391.209 153.475, 401 157.639 C 412.871 162.686, 420.407 166.569, 426.099 170.569 C 428.783 172.456, 431.241 174, 431.560 174 C 432.032 174, 441.123 181.147, 443.009 183 C 445.810 185.754, 449 188.200, 449 187.594 C 449 186.623, 435.144 173.112, 428 167.116 C 424.975 164.577, 422.275 162.272, 422 161.994 C 419.591 159.558, 406.001 150.317, 400.684 147.500 C 398.608 146.400, 395.749 144.575, 394.330 143.445 L 391.751 141.390 395.803 129.945 C 398.031 123.650, 401.261 114.675, 402.980 110 C 404.700 105.325, 407.257 98.125, 408.664 94 C 410.071 89.875, 411.779 85.150, 412.459 83.500 C 413.140 81.850, 414.500 78.025, 415.482 75 C 416.464 71.975, 417.854 68.150, 418.571 66.500 C 419.289 64.850, 421.091 59.900, 422.576 55.500 C 424.062 51.100, 426.520 44.012, 428.039 39.748 C 429.559 35.484, 430.393 31.743, 429.894 31.434 C 429.394 31.126, 428.703 31.689, 428.358 32.687 C 428.013 33.684, 426.891 36.470, 425.865 38.877 C 424.839 41.285, 424 44.070, 424 45.068 C 424 46.066, 423.550 47.160, 423 47.500 C 422.450 47.840, 422 48.998, 422 50.073 C 422 51.148, 421.549 52.809, 420.998 53.764 C 420.446 54.719, 417.065 63.600, 413.483 73.500 C 409.901 83.400, 406.121 93.750, 405.081 96.500 C 404.042 99.250, 400.111 109.927, 396.346 120.227 C 392.581 130.527, 388.977 139.161, 388.337 139.413 C 387.697 139.664, 384.091 138.393, 380.324 136.588 C 371.601 132.408, 358.347 127.534, 350 125.438 C 346.425 124.541, 343.152 123.480, 342.727 123.080 C 341.978 122.377, 339.508 115.807, 332.547 96 C 330.710 90.775, 327.779 82.675, 326.034 78 C 324.288 73.325, 321.551 65.675, 319.952 61 C 315.819 48.917, 313.424 42.239, 311.169 36.500 L 309.203 31.500 295.480 31.221 L 281.756 30.942 282.259 33.221 C 282.536 34.474, 285.109 42.025, 287.976 50 C 290.842 57.975, 293.556 65.625, 294.005 67 C 294.454 68.375, 296.267 73.325, 298.033 78 C 303.254 91.818, 303.506 93.101, 301.763 97.035 C 300.919 98.941, 299.207 103.425, 297.958 107 C 296.710 110.575, 295.329 113.908, 294.888 114.407 C 294.411 114.946, 285.265 114.630, 272.293 113.626 C 260.307 112.698, 247.125 111.840, 243 111.719 L 235.500 111.500 230.764 98.500 C 228.159 91.350, 224.884 82.125, 223.486 78 C 222.088 73.875, 220.551 69.600, 220.072 68.500 C 219.592 67.400, 217.535 61.775, 215.500 56 C 213.466 50.225, 210.607 42.350, 209.147 38.500 L 206.494 31.500 192.625 31.221 L 178.756 30.942 179.286 33.221 C 179.577 34.474, 181.243 39.325, 182.988 44 C 184.733 48.675, 187.455 56.325, 189.037 61 C 190.620 65.675, 193.770 74.675, 196.038 81 C 198.306 87.325, 201.171 95.533, 202.405 99.240 C 203.638 102.948, 204.952 106.285, 205.324 106.657 C 205.696 107.029, 206 108.283, 206 109.444 C 206 111.474, 205.148 111.623, 183.440 113.384 C 171.032 114.390, 160.619 114.952, 160.300 114.633 C 159.981 114.314, 161.342 112.257, 163.325 110.062 C 170.107 102.556, 174.951 89.580, 174.985 78.831 C 175.019 68.311, 169.542 54.431, 162.213 46.462 C 155.318 38.964, 149.584 35.501, 137.332 31.433 C 128.146 28.384, 116.773 27.915, 76 28.905 M 301.450 107.548 C 299.843 111.955, 299.072 115.444, 299.598 115.931 C 300.782 117.026, 310.929 117.924, 311.509 116.985 C 312.069 116.079, 305.784 100, 304.870 100 C 304.503 100, 302.964 103.397, 301.450 107.548 M 243 132.032 C 243 132.600, 243.708 134.737, 244.573 136.782 C 245.438 138.827, 247.241 143.875, 248.578 148 C 249.916 152.125, 251.411 156.400, 251.901 157.500 C 252.392 158.600, 254.230 163.550, 255.987 168.500 C 262.436 186.674, 264.713 192.404, 265.602 192.701 C 266.604 193.035, 268.498 189.150, 269.598 184.500 C 269.988 182.850, 271.872 177.450, 273.785 172.500 C 282.167 150.800, 287.846 134.453, 287.200 133.885 C 286.355 133.143, 258.023 130.984, 249.250 130.994 C 245.625 130.997, 243 131.433, 243 132.032 M 350 144.773 C 350 145.198, 351.308 148.942, 352.906 153.094 C 354.505 157.245, 357.479 165.560, 359.514 171.571 C 361.550 177.582, 363.800 183.850, 364.514 185.500 C 365.228 187.150, 366.088 189.738, 366.425 191.250 C 367.514 196.137, 369.428 194.192, 372.825 184.746 C 381.906 159.497, 384 153.502, 384 152.759 C 384 151.692, 379.587 150.030, 374 148.993 C 371.525 148.534, 366.125 147.230, 362 146.096 C 354.127 143.932, 350 143.477, 350 144.773 M 17.905 173.929 C 14.717 177.449, 14.856 178.113, 18.308 175.850 C 20.247 174.579, 21.619 173.193, 21.358 172.770 C 20.655 171.632, 19.691 171.956, 17.905 173.929"/></svg>
        </a>
        <nav class="nav-links" id="navLinks" aria-label="Primary">
          <a href="/#about">About Us</a>
          <a href="/#projects">Projects</a>
          <a href="/#services">Services</a>
          <a href="/#why-us">Why Us</a>
          <a href="/#process">Process</a>
        </nav>
        <div class="nav-right">
          <a href="tel:<%= c.contact.phone.replace(/\s/g, '') %>" class="nav-icon" aria-label="Call us">
            <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.8 19.8 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2.12 4.18 2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92Z"/></svg>
          </a>
          <a href="/#contact" class="btn-navy nav-cta">Get In Touch</a>
          <button class="nav-toggle" id="navToggle" aria-label="Toggle menu" aria-expanded="false">
            <span></span><span></span><span></span>
          </button>
        </div>
      </div>
    </header>

    <main class="pp<%= (project.sequence || project.video) ? ' pp-cinematic' : '' %>">
      <% if (project.sequence) { %>
      <!-- ============ SCROLL-SCRUB SEQUENCE HERO ============ -->
      <section class="pp-seq" id="ppSeq"
        data-base="<%= project.sequence.base %>" data-pad="<%= project.sequence.pad %>"
        data-ext="<%= project.sequence.ext %>" data-count="<%= project.sequence.count %>">
        <div class="pp-seq-sticky">
          <img class="pp-seq-fallback" src="<%= project.sequence.poster %>" alt="<%= project.title %>" />
          <canvas class="pp-seq-canvas" id="ppSeqCanvas"></canvas>
          <div class="pp-hero-overlay"></div>
          <div class="container pp-hero-inner" id="ppHeroInner">
            <a href="/#projects" class="pp-back">← All Projects</a>
            <% if (project.status) { %><span class="pp-status"><%= project.status %></span><% } %>
            <span class="pp-type"><%= project.type %></span>
            <h1 class="pp-title"><%= project.title %></h1>
            <div class="pp-hero-actions">
              <a href="/#contact" class="btn-navy">Enquire Now</a>
              <a href="#vision" class="btn-ghost-light">Explore the Project</a>
            </div>
          </div>
          <div class="pp-hero-scroll" aria-hidden="true">
            <span>Scroll to build</span>
            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="1.3"><path d="m6 9 6 6 6-6"/></svg>
          </div>
        </div>
      </section>
      <% } else { %>
      <!-- ============ PROJECT HERO (cinematic) ============ -->
      <section class="pp-hero" id="ppHero">
        <div class="pp-hero-media" id="ppHeroMedia">
          <div class="pp-hero-media-inner">
            <% if (project.video) { %>
            <video id="ppHeroVideo" autoplay muted loop playsinline webkit-playsinline preload="auto" poster="<%= project.image %>">
              <source src="<%= project.video %>" type="video/mp4" />
            </video>
            <% } else { %>
            <img src="<%= project.image %>" alt="<%= project.title %>" />
            <% } %>
          </div>
        </div>
        <div class="pp-hero-overlay"></div>
        <div class="container pp-hero-inner" id="ppHeroInner">
          <a href="/#projects" class="pp-back">← All Projects</a>
          <% if (project.status) { %><span class="pp-status"><%= project.status %></span><% } %>
          <span class="pp-type"><%= project.type %></span>
          <h1 class="pp-title"><%= project.title %></h1>
          <div class="pp-hero-actions">
            <a href="/#contact" class="btn-navy">Enquire Now</a>
            <a href="#vision" class="btn-ghost-light">Explore the Project</a>
          </div>
        </div>
        <div class="pp-hero-scroll" aria-hidden="true">
          <span>Scroll</span>
          <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="1.3"><path d="m6 9 6 6 6-6"/></svg>
        </div>
      </section>
      <% } %>

      <!-- ============ FROM VISION TO COMPLETION ============ -->
      <% if (c.process && c.process.items && c.process.items.length) { %>
      <section class="section pp-vision" id="vision">
        <div class="container">
          <div class="label reveal">Construction Journey <span class="label-line"></span></div>
          <h2 class="section-title reveal">From Vision to Completion</h2>
          <p class="pp-vision-lead reveal">Every Bridgeway development moves through a disciplined, transparent build process — from first survey to final handover.</p>
          <div class="pp-stages">
            <% c.process.items.forEach(function (s, i) { %>
            <div class="pp-stage-step reveal build" style="transition-delay: <%= (i % 5) * 0.08 %>s">
              <span class="pp-stage-num"><%= s.num %></span>
              <h3><%= s.title %></h3>
              <p><%= s.description %></p>
            </div>
            <% }); %>
          </div>
        </div>
      </section>
      <% } %>

      <!-- ============ OVERVIEW ============ -->
      <section class="section pp-overview">
        <div class="container pp-overview-grid">
          <div class="pp-overview-main">
            <div class="label reveal">Overview <span class="label-line"></span></div>
            <%
              var paras = (project.longDescription && project.longDescription.trim())
                ? project.longDescription.split(/\n+/) : [project.description];
              paras.forEach(function(par){ if (par && par.trim()) { %>
            <p class="pp-lead reveal"><%= par %></p>
            <% } }); %>
          </div>

          <% if (project.details && project.details.length) { %>
          <aside class="pp-specs reveal build">
            <h3>Project Details</h3>
            <ul>
              <% project.details.forEach(function(d){ %>
              <li><span class="pp-spec-label"><%= d.label %></span><span class="pp-spec-value"><%= d.value %></span></li>
              <% }); %>
            </ul>
          </aside>
          <% } %>
        </div>
      </section>

      <!-- ============ FEATURES ============ -->
      <% if (project.features && project.features.length) { %>
      <section class="section pp-features">
        <div class="container">
          <div class="label reveal">Highlights <span class="label-line"></span></div>
          <h2 class="section-title reveal">Key Features</h2>
          <ul class="pp-feature-grid">
            <% project.features.forEach(function(f, i){ %>
            <li class="reveal build" style="transition-delay: <%= (i % 3) * 0.08 %>s">
              <span class="pp-feat-ic">✦</span><%= f %>
            </li>
            <% }); %>
          </ul>
        </div>
      </section>
      <% } %>

      <!-- ============ GALLERY (3D construction assembly) ============ -->
      <%
        var media = [];
        if (project.gallery && project.gallery.length) media = project.gallery.slice();
        else if (project.image) media = [project.image];
      %>
      <% if (media.length) { %>
      <section class="section pp-gallery-sec">
        <div class="container">
          <div class="label reveal">Gallery <span class="label-line"></span></div>
          <h2 class="section-title reveal">Photos &amp; Renders</h2>
        </div>
        <div class="pp-stage">
          <div class="pp-gallery">
            <% media.forEach(function(url, i){
                 var isVid = /\.(mp4|webm|mov|ogg|m4v)(\?|$)/i.test(url); %>
            <figure class="pp-tile reveal build" style="transition-delay: <%= (i % 4) * 0.1 %>s">
              <% if (isVid) { %>
              <video src="<%= url %>" muted loop playsinline autoplay></video>
              <% } else { %>
              <img src="<%= url %>" alt="<%= project.title %> — image <%= i + 1 %>" loading="lazy" />
              <% } %>
            </figure>
            <% }); %>
          </div>
        </div>
      </section>
      <% } %>

      <!-- ============ CTA ============ -->
      <section class="section pp-cta">
        <div class="container pp-cta-inner reveal">
          <h2>Interested in <%= project.title %>?</h2>
          <p>Speak with our Lahore team about availability, pricing and a private viewing.</p>
          <div class="pp-cta-actions">
            <a href="/#contact" class="btn-navy">Enquire Now</a>
            <a href="tel:<%= c.contact.phone.replace(/\s/g, '') %>" class="btn-outline-dark">Call <%= c.contact.phone %></a>
          </div>
        </div>
      </section>
    </main>

    <!-- ============ FOOTER ============ -->
    <footer class="footer">
      <div class="container footer-grid">
        <div class="footer-brand">
          <span class="logo-text footer-logo"><%= c.footer.brandText %></span>
          <p><%= c.footer.description %></p>
        </div>
        <div class="footer-col"><h4>Company</h4><a href="/#about">About Us</a><a href="/#projects">Projects</a><a href="/#services">Services</a><a href="/#why-us">Why Us</a></div>
        <div class="footer-col"><h4>Services</h4><a href="/#services">Development</a><a href="/#services">Apartments</a><a href="/#services">Residential</a><a href="/#services">Commercial</a></div>
        <div class="footer-col"><h4>Contact</h4><a href="tel:<%= c.contact.phone.replace(/\s/g, '') %>"><%= c.contact.phone %></a><a href="mailto:<%= c.contact.email %>"><%= c.contact.email %></a><span><%= c.contact.address %></span></div>
      </div>
      <div class="footer-bottom container">
        <p>© <span id="year"></span> <%= c.site.brandName %>. All rights reserved.</p>
        <p class="footer-socials"><% c.footer.socials.forEach(function(s){ %><a href="<%= s.url %>"><%= s.label %></a><% }); %></p>
      </div>
    </footer>

    <button class="back-to-top" id="backToTop" aria-label="Back to top">↑</button>
    <script src="/script.js"></script>
    <script>
      // Cinematic hero: ensure video plays, then drive a slow scroll-linked
      // parallax (scale up + drift), text fade-out, and an architectural
      // blur/darken transition into the section below.
      (function () {
        var v = document.getElementById('ppHeroVideo');
        if (v) {
          v.muted = true; v.playsInline = true;
          var tryPlay = function () { var p = v.play(); if (p && p.catch) p.catch(function () {}); };
          tryPlay();
          v.addEventListener('canplay', tryPlay);
          document.addEventListener('touchstart', tryPlay, { once: true, passive: true });
        }
        var hero = document.getElementById('ppHero');
        var media = document.getElementById('ppHeroMedia');
        var inner = document.getElementById('ppHeroInner');
        var reduce = matchMedia('(prefers-reduced-motion: reduce)').matches;
        if (!hero || !media || reduce) return;

        var ticking = false;
        function onScroll() {
          if (ticking) return;
          ticking = true;
          requestAnimationFrame(function () {
            var h = hero.offsetHeight || window.innerHeight;
            var y = window.scrollY;
            var p = Math.max(0, Math.min(y / h, 1)); // 0 → 1 across the hero
            // Video slowly scales up and drifts upward for depth.
            media.style.transform = 'scale(' + (1 + p * 0.14).toFixed(4) + ') translateY(' + (-(p * 26)).toFixed(2) + 'px)';
            // Gentle blur + darken as the next section arrives.
            media.style.filter = 'brightness(' + (1 - p * 0.45).toFixed(3) + ') blur(' + (p * 4).toFixed(2) + 'px)';
            // Overlay text fades up and out.
            if (inner) {
              inner.style.opacity = Math.max(0, 1 - p * 1.5).toFixed(3);
              inner.style.transform = 'translateY(' + (-(p * 40)).toFixed(2) + 'px)';
            }
            ticking = false;
          });
        }
        window.addEventListener('scroll', onScroll, { passive: true });
        window.addEventListener('resize', onScroll);
        onScroll();
      })();

      // Scroll-scrub sequence hero: the building "constructs" as you scroll.
      (function () {
        var section = document.getElementById('ppSeq');
        var canvas = document.getElementById('ppSeqCanvas');
        if (!section || !canvas) return;
        var sticky = canvas.parentNode;
        var fallback = section.querySelector('.pp-seq-fallback');
        var inner = document.getElementById('ppHeroInner');
        var ctx = canvas.getContext('2d');
        var base = section.getAttribute('data-base');
        var ext = section.getAttribute('data-ext') || '.jpg';
        var padN = parseInt(section.getAttribute('data-pad'), 10) || 3;
        var count = parseInt(section.getAttribute('data-count'), 10) || 0;
        var reduce = matchMedia('(prefers-reduced-motion: reduce)').matches;
        var dpr = Math.min(window.devicePixelRatio || 1, 2);
        var images = new Array(count);
        var current = -1;

        function padStr(n) { var s = '' + n; while (s.length < padN) s = '0' + s; return s; }
        function nearest(i) {
          if (images[i] && images[i].naturalWidth) return images[i];
          for (var d = 1; d < count; d++) {
            if (images[i - d] && images[i - d].naturalWidth) return images[i - d];
            if (images[i + d] && images[i + d].naturalWidth) return images[i + d];
          }
          return null;
        }
        // Contain-fit: show the WHOLE photo (no cropping), centered.
        function drawContain(img) {
          var cw = canvas.width, ch = canvas.height, iw = img.naturalWidth, ih = img.naturalHeight;
          var s = Math.min(cw / iw, ch / ih), w = iw * s, h = ih * s;
          ctx.clearRect(0, 0, cw, ch);
          ctx.drawImage(img, (cw - w) / 2, (ch - h) / 2, w, h);
        }
        function draw(i, force) {
          i = Math.max(0, Math.min(count - 1, i));
          if (i === current && !force) return;
          current = i;
          var img = nearest(i);
          if (img) { drawContain(img); if (fallback) fallback.style.opacity = '0'; }
        }
        function resize() {
          var w = sticky.clientWidth, h = sticky.clientHeight;
          canvas.width = Math.round(w * dpr); canvas.height = Math.round(h * dpr);
          canvas.style.width = w + 'px'; canvas.style.height = h + 'px';
          draw(current < 0 ? 0 : current, true);
        }

        // Preload all frames; draw as soon as the relevant ones arrive.
        for (var k = 0; k < count; k++) {
          (function (k) {
            var im = new Image();
            im.onload = function () { if (k === 0 || Math.abs(k - current) <= 1) draw(current < 0 ? 0 : current, true); };
            im.src = base + padStr(k + 1) + ext;
            images[k] = im;
          })(k);
        }

        resize();
        window.addEventListener('resize', resize);

        if (reduce) {
          // No scrubbing — collapse to a single static frame.
          section.style.height = '100svh';
          var mid = new Image();
          mid.onload = function () { draw(Math.floor(count / 2), true); };
          mid.src = base + padStr(Math.floor(count / 2) + 1) + ext;
          return;
        }

        var ticking = false;
        function onScroll() {
          if (ticking) return;
          ticking = true;
          requestAnimationFrame(function () {
            var scrollable = section.offsetHeight - window.innerHeight;
            var p = scrollable > 0 ? (-section.getBoundingClientRect().top) / scrollable : 0;
            p = Math.max(0, Math.min(1, p));
            draw(Math.round(p * (count - 1)));
            if (inner) inner.style.opacity = (p < 0.78 ? 1 : Math.max(0, 1 - (p - 0.78) / 0.22)).toFixed(3);
            ticking = false;
          });
        }
        window.addEventListener('scroll', onScroll, { passive: true });
        onScroll();
      })();
    </script>
  </body>
</html>
